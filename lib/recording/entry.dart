class Entry {
  // skin reactions per minute
  late int scrn;
  // moodmetric level
  late int mm;
  // raw level of skin conductance
  late int scl;
  // amount of steps
  late int steps;

  Entry(this.scrn, this.mm, this.scl, this.steps);
}
